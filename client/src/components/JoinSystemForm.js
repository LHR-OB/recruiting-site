import { React, useState } from 'react';
import { usersApi } from '../api/endpoints/users';

export default function JoinSystemForm() {
  // States
  const [systems, setSystems] = useState([]);

  const handleJoinSystem = () => {
    
  }
}
