import { React } from 'react';
import {
  Container,
  Typography,
} from '@mui/material';

export default function ApplicationInfo() {
  return (
    <Container>
      <Typography variant="h6" mt={2}>
        Application Info
      </Typography>
    </Container>
  );
}
